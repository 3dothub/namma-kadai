import { View } from 'react-native';

// This is a placeholder component for the add tab
// The actual functionality is handled in the tab bar listener
export default function AddScreen() {
  return <View style={{ flex: 1 }} />;
}
